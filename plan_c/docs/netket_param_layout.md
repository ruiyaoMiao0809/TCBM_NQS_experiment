# NetKet RBM parameter layout (Plan C, Day 8)
> Status: **待 Nick / AI review**. Auto-generated by `day8_smoke.py`.

## Energy convention (F-level, ratified Nick+AI Day 8)

- NetKet `nk.operator.Heisenberg` = **sigma.sigma (Pauli)** convention.
- Phase A ED / truth `E0 = -8.4579` = **S.S (spin-1/2, S=sigma/2)**; `S.S = (1/4) sigma.sigma`.
- Whole experiment line unified to **S.S** scale: the single entry `plan_c.core.j1j2_system.build_j1j2_hamiltonian` passes **J/4** to NetKet so energies are native S.S. `/4` appears ONLY there; callers use physical J1=1.0, J2=0.5.
- `E0_target = -8.4579`; guarded by `plan_c/tests/test_energy_convention.py`.

- netket 3.21.0, jax 0.8.3, backend cpu
- model: `nk.models.RBM(alpha=2, param_dtype=complex)` on 4x4 J1-J2, Sz=0
- `vs.n_parameters = 560`; ravel flat = **560 complex = 1120 real**

## pytree structure (ravel_pytree order)

`jax.flatten_util.ravel_pytree(vs.parameters)` flattens leaves in `tree_flatten` order, row-major within each leaf:

| leaf key | shape | dtype | complex index range |
|----------|-------|-------|---------------------|
| `Dense/bias` | (32,) | complex128 | `[0:32]` |
| `Dense/kernel` | (16, 32) | complex128 | `[32:544]` |
| `visible_bias` | (16,) | complex128 | `[544:560]` |

## complex -> real mapping convention (interface for theory line)

Define the real parameter vector as:

```
flat_complex = ravel_pytree(vs.parameters)[0]   # shape (560,), complex128
theta_real   = concat([ Re(flat_complex), Im(flat_complex) ])  # shape (1120,)
```

Under this convention (Re block first, Im block second):

- **Re block** = `theta_real[0:560]`
- **P_Im (imaginary block)** = `theta_real[560:1120]`  = the **last 560 dims**

This is the unambiguous P_Im definition for the theory-line T4b projection.

## QGT ordering (STEP 5 result)

- QGT dense shape = `(1120, 1120)`, dtype = `float64` (via QGTJacobianDense).
- ravel complex dim = 560 (real = 1120); QGT dim 1120 == n_real -> real-split QGT.
- NetKet builds the Jacobian in the **same pytree leaf order as `ravel_pytree`** (parameter grouping matches the complex-index table above).
- **OPEN / NEEDS-VERIFY before Day13**: the real-split *layout within* the 1120 axes — block `[Re(0:560); Im(560:1120)]` vs interleaved `[Re_i, Im_i, ...]` — is NOT yet confirmed. This determines the P_Im <-> QGT-axis mapping for the Day13 projection. (Singular values come in degenerate pairs, consistent with a symmetric Re/Im split but not decisive on the ordering.) Verify explicitly in Day9-13.
